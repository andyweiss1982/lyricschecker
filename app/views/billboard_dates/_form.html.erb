<%= form_for(@billboard_date) do |f| %>
  <% if @billboard_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@billboard_date.errors.count, "error") %> prohibited this billboard_date from being saved:</h2>

      <ul>
      <% @billboard_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %><br>
    <%= f.text_field :day %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
