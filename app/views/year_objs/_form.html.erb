<%= form_for(@year_obj) do |f| %>
  <% if @year_obj.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@year_obj.errors.count, "error") %> prohibited this year_obj from being saved:</h2>

      <ul>
      <% @year_obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :num %><br>
    <%= f.number_field :num %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
